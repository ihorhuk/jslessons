// Задание:
// Напишите преобразование первого символа строки в нижний регистр.

const str = "Пример строки";
const result = str.charAt(0).toLowerCase() + str.slice(1); // тут ваш код вместо null

console.log(result); // пример строки
